# nginx + certbot 웹서버 인프라 환경 변수
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요.

# ============================================
# Let's Encrypt SSL 인증서 설정
# ============================================

# SSL 인증서를 발급받을 도메인 목록 (쉼표로 구분)
# 예시: mysite.com,api.mysite.com,blog.mysite.com
CERTBOT_DOMAIN=aphennet.likeweb.co.kr,aphennetapi.likeweb.co.kr

# SSL 인증서 발급 시 사용할 이메일 주소
CERTBOT_EMAIL=ohsjwe@gmail.com

# 테스트 환경 사용 여부 (0: 프로덕션, 1: 테스트)
# 처음 설정할 때는 1로 설정해서 테스트 후 0으로 변경하세요
CERTBOT_STAGING=1

# ============================================
# nginx 설정 (선택사항 - 고급 사용자용)
# ============================================

# nginx 워커 프로세스 수 (기본값: auto)
# NGINX_WORKER_PROCESSES=auto

# nginx 워커 연결 수 (기본값: 1024)
# NGINX_WORKER_CONNECTIONS=1024

# ============================================
# 로그 설정 (선택사항 - 고급 사용자용)
# ============================================

# 로그 레벨 (debug, info, notice, warn, error, crit, alert, emerg)
# NGINX_LOG_LEVEL=warn

# 로그 파일 경로
# NGINX_ACCESS_LOG=/var/log/nginx/access.log
# NGINX_ERROR_LOG=/var/log/nginx/error.log
